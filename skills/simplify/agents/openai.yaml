interface:
  display_name: "Simplify"
  short_description: "Review and simplify changed code safely"
  default_prompt: "Use $simplify to review changed code for evidence-backed cleanup opportunities and apply only selected fixes."
