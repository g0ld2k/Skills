interface:
  display_name: "Simplify"
  short_description: "Review changed code quality"
  default_prompt: "Use $simplify to review the current changes for reuse, quality, and efficiency issues."
policy:
  allow_implicit_invocation: false
